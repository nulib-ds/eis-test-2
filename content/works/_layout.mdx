---
type: work
---

import {
  Label,
  Summary,
  Metadata,
  RequiredStatement,
} from "@canopy-iiif/app/ui/server";

<article className="space-y-12">

<Viewer iiifContent={props.manifest.id} />

<header className="space-y-4">
  <Label
    manifest={props.manifest}
    as="h1"
    className="text-4xl font-semibold tracking-tight text-slate-900"
  />
  <Summary
    manifest={props.manifest}
    as="p"
    className="max-w-3xl text-lg text-slate-700"
  />
</header>

{Array.isArray(props.manifest?.metadata) && props.manifest.metadata.length ? (

<section className="space-y-4">
  <h2 className="text-2xl font-semibold text-slate-900">Details</h2>
  <Metadata
    manifest={props.manifest}
    className="grid gap-x-12 gap-y-6 md:grid-cols-2"
  />
</section>
) : null}

## Annotations

{props.manifest?.requiredStatement ? (

<section className="space-y-3">
  <h2 className="text-xl font-semibold text-slate-900">Rights</h2>
  <RequiredStatement
    manifest={props.manifest}
    className="text-sm text-slate-700"
  />
</section>
) : null}

<Scroll iiifContent={props.manifest.id} />

  <section className="space-y-4">
    <h2 className="text-2xl font-semibold text-slate-900">Related Items</h2>
    <RelatedItems iiifContent={props.manifest.id} top={3} />
  </section>
</article>
